# Web API

The Web API is a ASP.NET Core Web API project. It contains everything related to the API.

## Debugging

Start the Web API in watch mode. This means that it will automatically reload on change.

```cmd
dotnet watch run
```
