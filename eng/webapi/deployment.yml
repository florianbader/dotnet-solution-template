stages:
  - template: ../templates/deployment/locations-deployment.yml
    parameters:
      dependsOn: Build
      environments:
        dev:
          name: Development
          locations:
            euw: West Europe
          jobs:
            - template: ./deployment/deployment-env.yml

        prev:
          name: Preview
          dependsOn: dev
          locations:
            euw: West Europe
          jobs:
            - template: ./deployment/deployment-env.yml

        prod:
          name: Production
          dependsOn: prev
          locations:
            euw: West Europe
          jobs:
            - template: ./deployment/deployment-env.yml
